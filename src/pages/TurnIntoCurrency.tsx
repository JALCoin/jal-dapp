import type { FC } from 'react';

const TurnIntoCurrency: FC = () => {
  return (
    <main>
      <div className="container">
        <h1>Finalize Your Token</h1>
        <p>This is where you'll upload a logo and metadata to turn your token into a real crypto asset.</p>
        <p>Coming soon...</p>
      </div>
    </main>
  );
};

export default TurnIntoCurrency;
